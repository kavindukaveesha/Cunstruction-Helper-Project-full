import 'package:flutter/material.dart';

class SplashAnimatedWidget extends StatelessWidget {
  const SplashAnimatedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
